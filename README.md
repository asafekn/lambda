# Lambda

This is a repository for implementations of the lambda calculus

## REPL

Run the read-evaluate-print-loop.

``` bash
$ cabal run
```

## Evaluate a file

``` bash
$ cabal run lambda -- ./test.lbd
```
